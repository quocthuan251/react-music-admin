import React from 'react';

class DashBoard extends React.Component {
	constructor(props) {
		super(props);

		// We declare the state as shown below

		this.state = {
			x: 'This is x from state',
			y: 'This is y from state',
		};
	}
	render() {
		return (
			<div>
				<h1>{this.state.x}</h1>
				<h2>{this.state.y}</h2>
				<div>
					Đừng lo lắng, mình sẽ giải thích đoạn code trên ngay sau
					đây: watchesSaga: 1 generator function sẽ gửi action được
					dispatched tới Store. takeLatest: tức là nếu chúng ta thực
					hiện 1 loạt các actions chúng sẽ chỉ thực thi và để trả lại
					kết quả cuối cung fetchProduct function request lên API
					workerSaga generator function dùng để gọi hàm call lên api
					và trả về kết quả nếu fetchProduct thành công dispatch type
					action và dữ liệu product trong payload store nếu
					fetchProduct lỗi dispatch type action và lỗi put để dispatch
					OK giải thích đã xong. Giờ cần mock React, Redux, Redux-Saga
					vào. Giờ bạn thấy react, redux, redux-saga đang riêng biệt.
					ở đâu mình phải gom chúng lại điều này cần import
					react-redux yarn add react-redux // import react-redux
					index.js các bạn sẽ viết ntn:Đừng lo lắng, mình sẽ giải
					thích đoạn code trên ngay sau đây: watchesSaga: 1 generator
					function sẽ gửi action được dispatched tới Store.
					takeLatest: tức là nếu chúng ta thực hiện 1 loạt các actions
					chúng sẽ chỉ thực thi và để trả lại kết quả cuối cung
					fetchProduct function request lên API workerSaga generator
					function dùng để gọi hàm call lên api và trả về kết quả nếu
					fetchProduct thành công dispatch type action và dữ liệu
					product trong payload store nếu fetchProduct lỗi dispatch
					type action và lỗi put để dispatch OK giải thích đã xong.
					Giờ cần mock React, Redux, Redux-Saga vào. Giờ bạn thấy
					react, redux, redux-saga đang riêng biệt. ở đâu mình phải
					gom chúng lại điều này cần import react-redux yarn add
					react-redux // import react-redux index.js các bạn sẽ viết
					ntn:Đừng lo lắng, mình sẽ giải thích đoạn code trên ngay sau
					đây: watchesSaga: 1 generator function sẽ gửi action được
					dispatched tới Store. takeLatest: tức là nếu chúng ta thực
					hiện 1 loạt các actions chúng sẽ chỉ thực thi và để trả lại
					kết quả cuối cung fetchProduct function request lên API
					workerSaga generator function dùng để gọi hàm call lên api
					và trả về kết quả nếu fetchProduct thành công dispatch type
					action và dữ liệu product trong payload store nếu
					fetchProduct lỗi dispatch type action và lỗi put để dispatch
					OK giải thích đã xong. Giờ cần mock React, Redux, Redux-Saga
					vào. Giờ bạn thấy react, redux, redux-saga đang riêng biệt.
					ở đâu mình phải gom chúng lại điều này cần import
					react-redux yarn add react-redux // import react-redux
					index.js các bạn sẽ viết ntn:Đừng lo lắng, mình sẽ giải
					thích đoạn code trên ngay sau đây: watchesSaga: 1 generator
					function sẽ gửi action được dispatched tới Store.
					takeLatest: tức là nếu chúng ta thực hiện 1 loạt các actions
					chúng sẽ chỉ thực thi và để trả lại kết quả cuối cung
					fetchProduct function request lên API workerSaga generator
					function dùng để gọi hàm call lên api và trả về kết quả nếu
					fetchProduct thành công dispatch type action và dữ liệu
					product trong payload store nếu fetchProduct lỗi dispatch
					type action và lỗi put để dispatch OK giải thích đã xong.
					Giờ cần mock React, Redux, Redux-Saga vào. Giờ bạn thấy
					react, redux, redux-saga đang riêng biệt. ở đâu mình phải
					gom chúng lại điều này cần import react-redux yarn add
					react-redux // import react-redux index.js các bạn sẽ viết
					fetchProduct thành công dispatch type action và dữ liệu
					product trong payload store nếu fetchProduct lỗi dispatch
					type action và lỗi put để dispatch OK giải thích đã xong.
					Giờ cần mock React, Redux, Redux-Saga vào. Giờ bạn thấy
					react, redux, redux-saga đang riêng biệt. ở đâu mình phải
					gom chúng lại điều này cần import react-redux yarn add
					fetchProduct thành công dispatch type action và dữ liệu
					product trong payload store nếu fetchProduct lỗi dispatch
					type action và lỗi put để dispatch OK giải thích đã xong.
					Giờ cần mock React, Redux, Redux-Saga vào. Giờ bạn thấy
					react, redux, redux-saga đang riêng biệt. ở đâu mình phải
					gom chúng lại điều này cần import react-redux yarn add
					fetchProduct thành công dispatch type action và dữ liệu
					product trong payload store nếu fetchProduct lỗi dispatch
					type action và lỗi put để dispatch OK giải thích đã xong.
					Giờ cần mock React, Redux, Redux-Saga vào. Giờ bạn thấy
					react, redux, redux-saga đang riêng biệt. ở đâu mình phải
					gom chúng lại điều này cần import react-redux yarn add
					fetchProduct thành công dispatch type action và dữ liệu
					product trong payload store nếu fetchProduct lỗi dispatch
					type action và lỗi put để dispatch OK giải thích đã xong.
					Giờ cần mock React, Redux, Redux-Saga vào. Giờ bạn thấy
					react, redux, redux-saga đang riêng biệt. ở đâu mình phải
					gom chúng lại điều này cần import react-redux yarn add ntn:
				</div>
			</div>
		);
	}
}
export default DashBoard;
